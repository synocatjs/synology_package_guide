#!/bin/bash

### Use PKG_DIR as working directory.
PKG_DIR=/tmp/_test_spk
rm -rf $PKG_DIR
mkdir -p $PKG_DIR

### get spk packing functions
source /pkgscripts/include/pkg_util.sh

create_package_tgz() {
	local package_tgz_dir=/tmp/_package_tgz
	local binary_dir=$package_tgz_dir/usr/local/bin

	### clear destination directory
	rm -rf $package_tgz_dir && mkdir -p $package_tgz_dir

	### install demo binary
	mkdir -p $binary_dir
	
	if [ -f /source/basic/demo ]; then
		cp -av /source/basic/demo $binary_dir
	else
		echo "Error: basic demo binary not found!"
		exit 1
	fi

	### create package.tgz
	pkg_make_package $package_tgz_dir "${PKG_DIR}"
}

create_spk(){
	# 生成 INFO 文件
	if [ -f /source/basic/INFO.sh ]; then
		cd /source/basic
		./INFO.sh > INFO
		cd /source/basic/SynoBuildConf
	fi
	
	# 复制文件
	[ -d /source/scripts ] && cp -av /source/scripts $PKG_DIR/scripts
	ls /source/PACKAGE_ICON*.PNG 2>/dev/null && cp -av /source/PACKAGE_ICON*.PNG $PKG_DIR
	[ -d /source/conf ] && cp -av /source/conf $PKG_DIR
	
	# 复制 INFO
	[ -f /source/INFO ] && cp /source/INFO $PKG_DIR/INFO

	### Create the final spk
	mkdir -p /image/packages
	pkg_make_spk ${PKG_DIR} "/image/packages" $(pkg_get_spk_family_name)
}

main() {
	create_package_tgz
	create_spk
}

main "$@"